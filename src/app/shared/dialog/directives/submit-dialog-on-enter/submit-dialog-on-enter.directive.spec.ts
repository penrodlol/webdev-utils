import { SubmitDialogOnEnterDirective } from './submit-dialog-on-enter.directive';

describe('SubmitDialogOnEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new SubmitDialogOnEnterDirective();
    expect(directive).toBeTruthy();
  });
});
