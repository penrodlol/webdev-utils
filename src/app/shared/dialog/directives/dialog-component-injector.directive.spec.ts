import { DialogComponentInjectorDirective } from './dialog-component-injector.directive';

describe('DialogComponentInjectorDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogComponentInjectorDirective();
    expect(directive).toBeTruthy();
  });
});
